import 'dart:ui';

import 'package:injectable/injectable.dart';

@injectable
class AppConstants {
  /// --------------------------------------------------------------------------
  /// APP NAME
  /// --------------------------------------------------------------------------
  final appName = "MoniePoint Assessment";

  /// --------------------------------------------------------------------------
  /// BASE CONSTANTS
  /// --------------------------------------------------------------------------
  final designSize = const Size(360, 800);
}
